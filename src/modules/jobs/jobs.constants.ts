export const JOB_SORTABLE_FIELDS = [
  "createdAt",
  "updatedAt",
  "role",
  "company",
  "status",
  "matchScore",
] as const;

export const DEFAULT_PAGE_SIZE = 20;
export const MAX_PAGE_SIZE = 100;
